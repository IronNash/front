import { Routes } from '@angular/router';

/**
 * Navigation is tab-driven on `App` for now. Add entries here when each area should have
 * its own URL (e.g. `/cte`, `/mdfe`) or lazy-loaded feature routes.
 */
export const routes: Routes = [];
